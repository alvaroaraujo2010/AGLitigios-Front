# Repo AGLitigios-Front (Angular en la raíz). En Netlify: Base y Package vacíos.

[build]
  command = "npm ci && npm run build"
  publish = "dist/frontend/browser"

[build.environment]
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
